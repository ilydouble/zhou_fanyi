# 🎭 周繁漪AI人脸融合H5应用

基于周繁漪定妆照的真实AI人脸融合Web应用，使用阿里云人脸融合API实现高质量的人脸融合效果。用户可以选择不同的周繁漪定妆照风格，上传自己的照片进行AI人脸融合。

## ✨ 功能特性

### 🤖 真实AI人脸融合
- **阿里云人脸融合API** - 使用专业的AI人脸融合服务
- **5个注册模板** - 周繁漪定妆照已注册为阿里云模板
- **高质量融合** - 真实的AI处理，效果自然逼真
- **即时结果** - 几秒钟内获得融合结果

### 📱 移动端优化
- 响应式设计，完美适配手机屏幕
- 支持触摸操作和手势
- 流畅的动画效果和过渡

### 📷 拍照上传
- 支持调用摄像头拍照
- 支持从相册选择照片
- 支持拖拽上传
- 自动图片预览和压缩

### 🎨 周繁漪定妆照模板
- 5种不同的周繁漪定妆照风格
- 实际定妆照缩略图预览
- 通过URL参数区分模板
- 每个模板独立的页面和风格

### 💾 结果处理
- 融合结果实时展示
- 自动下载到本地
- 支持重新制作和模板切换
- 结果图片本地保存

## 🚀 快速开始

### 1. 环境准备

确保已安装Python 3.7+和相关依赖：

```bash
pip install -r requirements.txt
```

### 2. 配置环境变量

在项目根目录的 `.env` 文件中确保包含：

```env
# 阿里云API密钥（人脸融合服务）
ALIYUN_ACCESS_KEY_ID=your_access_key_id
ALIYUN_ACCESS_KEY_SECRET=your_access_key_secret

# OSS配置（上海区域）
OSS_ACCESS_KEY_ID=your_access_key_id
OSS_ACCESS_KEY_SECRET=your_access_key_secret
OSS_BUCKET_NAME=your_bucket_name
OSS_ENDPOINT=oss-cn-shanghai.aliyuncs.com
OSS_BASE_PATH=liveportrait
```

### 3. 上传并注册模板

首次使用需要上传周繁漪定妆照并注册为阿里云模板：

```bash
# 上传模板到OSS
python simple_upload.py

# 注册模板到阿里云人脸融合服务
curl -X POST http://localhost:8081/api/register-templates
```

### 4. 启动应用

```bash
# 启动服务器
python web_server.py
```

### 5. 访问应用

- **主页**: http://localhost:8081/
- **手机访问**: http://192.168.1.24:8081 (同一WiFi网络)

## 📁 项目结构

```
web/
├── index.html              # 主页面 - 模板选择界面
├── fanyi.html              # 模板页面 - 人脸融合功能
├── app.js                  # 主页面逻辑
├── fanyi.js                # 模板页面逻辑
├── templates_config.json   # 模板配置文件（包含阿里云模板ID）
├── uploads/                # 临时上传目录
└── README.md              # 说明文档

../
├── web_server.py           # Flask后端服务
├── face_fusion_sdk.py      # 阿里云人脸融合SDK封装
├── oss_uploader.py         # OSS文件上传
├── simple_upload.py        # 模板上传脚本
├── pics/                   # 周繁漪定妆照原图
│   ├── fanyi-1.jpg         # 定妆照1
│   ├── fanyi-2.jpg         # 定妆照2
│   ├── fanyi-3.jpg         # 定妆照3
│   ├── fanyi-4.jpg         # 定妆照4
│   └── fanyi-5.jpg         # 定妆照5
├── images/results/         # 融合结果保存目录
└── requirements.txt        # Python依赖
```

## 🔗 访问路由

### 主页面
- **URL**: `/`
- **功能**: 显示5个周繁漪定妆照模板选择界面
- **特点**: 展示实际定妆照缩略图

### 模板页面
通过URL参数区分不同的周繁漪定妆照：

- **定妆照1**: `/fanyi?template=1` - 造型1
- **定妆照2**: `/fanyi?template=2` - 造型2
- **定妆照3**: `/fanyi?template=3` - 造型3
- **定妆照4**: `/fanyi?template=4` - 造型4
- **定妆照5**: `/fanyi?template=5` - 造型5

## 🔧 API接口

### 模板注册
```
POST /api/register-templates

功能: 将周繁漪定妆照注册为阿里云人脸融合模板

响应:
{
  "success": true,
  "data": {
    "totalTemplates": 5,
    "successCount": 5,
    "failedCount": 0,
    "registeredTemplates": [
      {
        "id": "1",
        "name": "周繁漪定妆照1",
        "aliyunTemplateId": "6a88468b-13b1-4dec-8ca6-956f492bb857",
        "status": "success"
      }
    ]
  }
}
```

### 模板信息
```
GET /api/template/{id}

响应:
{
  "success": true,
  "data": {
    "id": "1",
    "name": "周繁漪定妆照1",
    "templateUrl": "https://oss-url...",
    "aliyunTemplateId": "6a88468b-13b1-4dec-8ca6-956f492bb857",
    "registrationStatus": "success"
  }
}
```

### 文件上传
```
POST /api/upload
Content-Type: multipart/form-data

参数:
- file: 图片文件

响应:
{
  "success": true,
  "url": "https://oss-signed-url...",
  "message": "上传成功"
}
```

### 人脸融合（真实API）
```
POST /api/face-fusion
Content-Type: application/json

参数:
{
  "userImageUrl": "用户照片URL",
  "templateId": "模板ID (1-5)"
}

响应:
{
  "success": true,
  "data": {
    "imageUrl": "阿里云融合结果URL",
    "localImageUrl": "/images/results/user_xxx.jpg",
    "downloadUrl": "/download/user_xxx.jpg",
    "requestId": "阿里云请求ID",
    "source": "real_api"
  },
  "message": "融合成功"
}
```

## 🎨 周繁漪定妆照模板

### 当前模板

系统使用pics目录下的5张周繁漪定妆照作为融合模板：

1. **fanyi-1.jpg** - 定妆照1，造型1
2. **fanyi-2.jpg** - 定妆照2，造型2
3. **fanyi-3.jpg** - 定妆照3，造型3
4. **fanyi-4.jpg** - 定妆照4，造型4
5. **fanyi-5.jpg** - 定妆照5，造型5

### 模板注册流程

1. **上传到OSS**:
```bash
python simple_upload.py
```

2. **注册到阿里云**:
```bash
curl -X POST http://localhost:8081/api/register-templates
```

这会将pics目录下的定妆照上传到OSS并注册为阿里云人脸融合模板。

### 模板要求

- **格式**: JPG、PNG
- **分辨率**: 建议512x512或更高
- **大小**: < 20MB（阿里云限制）
- **内容**: 包含清晰的人脸，适合融合
- **命名**: fanyi-{数字}.jpg 格式
- **人脸要求**: 五官清晰，无遮挡，偏侧角度不超过30度

## 📱 移动端特性

### 拍照功能
- 支持前后摄像头切换
- 自动对焦和曝光
- 实时预览效果
- 支持相册选择

### 触摸优化
- 支持手势操作和拖拽上传
- 防误触设计
- 流畅的滑动体验
- 响应式布局适配各种屏幕

### 用户体验
- 模板缩略图预览
- 加载状态提示
- 错误处理和重试
- 一键重新制作
- 真实AI融合效果展示

## 🔒 安全考虑

### 文件安全
- 文件类型验证
- 文件大小限制
- 临时文件自动清理

### API安全
- 请求频率限制
- 参数验证
- 错误信息脱敏

### 隐私保护
- 用户照片加密传输
- 临时存储自动过期
- 不保存用户隐私信息

## 🚀 部署指南

### 本地开发
```bash
# 启动开发服务器
python web_server.py

# 或使用启动脚本
python start_web_app.py
```

### 生产部署
```bash
# 使用Gunicorn
pip install gunicorn
gunicorn -w 4 -b 0.0.0.0:8081 web_server:app

# 使用Docker
docker build -t fanyi-fusion-app .
docker run -p 8081:8081 fanyi-fusion-app
```

### Nginx配置
```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://127.0.0.1:8081;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    client_max_body_size 20M;  # 支持阿里云20MB限制
}
```

### 域名访问示例
- 主页: https://your-domain.com/
- 定妆照1: https://your-domain.com/fanyi?template=1
- 定妆照2: https://your-domain.com/fanyi?template=2
- 等等...

## 🐛 故障排除

### 常见问题

1. **模板不显示**
   - 运行 `python simple_upload.py` 上传模板
   - 运行模板注册API: `curl -X POST http://localhost:8081/api/register-templates`
   - 检查 `web/templates_config.json` 是否包含 `aliyunTemplateId`

2. **上传失败**
   - 检查OSS配置和密钥
   - 确认网络连接稳定
   - 验证文件格式和大小（<20MB）

3. **融合失败**
   - 检查模板是否已注册: `curl http://localhost:8081/api/template/1`
   - 验证阿里云API密钥和权限
   - 确认人脸检测通过（清晰人脸，无遮挡）

4. **页面无法访问**
   - 检查端口占用（默认8081）
   - 确认防火墙设置
   - 验证依赖安装完整

### 调试模式
```bash
# 启用调试模式
export FLASK_DEBUG=1
python web_server.py

# 测试API接口
curl http://localhost:8081/api/templates

# 测试模板信息
curl http://localhost:8081/api/template/1

# 测试模板注册
curl -X POST http://localhost:8081/api/register-templates
```

### 重新初始化
```bash
# 重新上传并注册模板
python simple_upload.py
curl -X POST http://localhost:8081/api/register-templates

# 清理并重启
rm web/templates_config.json
python simple_upload.py
curl -X POST http://localhost:8081/api/register-templates
python web_server.py
```

## 🎯 使用流程

### 完整操作步骤

1. **访问主页** - http://localhost:8081/
   - 查看5个周繁漪定妆照模板
   - 每个模板显示实际的定妆照缩略图

2. **选择模板** - 点击喜欢的定妆照
   - 自动跳转到对应的模板页面
   - 每个模板有独特的颜色主题

3. **上传照片** - 在模板页面上传用户照片
   - 支持拍照或选择相册
   - 自动预览和验证

4. **AI融合** - 点击"开始融合"
   - 调用阿里云真实AI人脸融合API
   - 显示处理进度（通常几秒钟完成）

5. **查看结果** - 融合完成后展示结果
   - 真实的AI融合效果
   - 自动下载到本地
   - 支持重新制作

### 分享链接

可以直接分享特定模板的链接：
- 定妆照1: http://your-domain.com/fanyi?template=1
- 定妆照2: http://your-domain.com/fanyi?template=2
- 定妆照3: http://your-domain.com/fanyi?template=3
- 定妆照4: http://your-domain.com/fanyi?template=4
- 定妆照5: http://your-domain.com/fanyi?template=5

## 📊 当前状态

- ✅ **模板系统** - 5个周繁漪定妆照模板已上传并注册
- ✅ **阿里云集成** - 真实人脸融合API已接入
- ✅ **文件上传** - OSS集成完成，支持签名URL
- ✅ **前端界面** - 响应式设计，移动端优化
- ✅ **路由系统** - URL参数区分模板
- ✅ **人脸融合** - 真实AI融合功能已实现
- ✅ **结果处理** - 自动下载和本地保存

## 🎯 技术特色

### 真实AI融合
- 使用阿里云专业人脸融合API
- 5个周繁漪定妆照已注册为官方模板
- 高质量融合效果，自然逼真

### 完整流程
- 用户上传 → OSS存储 → API融合 → 结果下载
- 全自动化处理，用户体验流畅
- 错误处理和重试机制完善

### 生产就绪
- 代码结构清晰，易于维护
- 完善的错误处理和日志记录
- 支持生产环境部署

## 📞 技术支持

如有问题，请查看：
1. 控制台日志 - 查看详细错误信息
2. API测试 - 使用curl测试接口
3. 模板状态 - 检查模板注册情况
4. 网络连接 - 确认阿里云API访问正常

## 📄 许可证

MIT License - 基于周繁漪定妆照的AI人脸融合应用
